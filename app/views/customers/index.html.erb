<div class="container">
<%= link_to 'Kunde hinzufügen', new_user_customer_path(current_user) %> |
<%= link_to 'Lagerbestand versenden', send_stock_update_user_customer_path(current_user, 1) %>
<hr>
<br>
<br>

<% if current_user.customers.any? %>
  <% current_user.customers.each do |customer| %>
    <div class="list-group">
      <div><%= customer.name %></div>
      <div><%= customer.email %>  </div>
    <%= link_to "#{customer.name} bearbeiten", edit_user_customer_path(current_user, customer),
    class: "list-group-item-text" %> |
    <%= link_to "#{customer.name} löschen", user_customer_path(current_user, customer), method: :delete, class: "list-group-item-text" %>
    <br><br>
  <% end %>
<% end %>

</div>
